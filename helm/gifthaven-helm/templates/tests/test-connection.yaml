apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gifthaven-helm.fullname" . }}-test-connection"
  labels:
    {{- include "gifthaven-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gifthaven-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

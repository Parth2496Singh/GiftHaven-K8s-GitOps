apiVersion: v1
kind: Service
metadata:
  name: {{ include "gifthaven-helm.fullname" . }}
  labels:
    {{- include "gifthaven-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gifthaven-helm.selectorLabels" . | nindent 4 }}
